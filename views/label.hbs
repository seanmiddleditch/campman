{{>header.hbs title=label.slug}}
<div class="page-header">
    <h1><i class="fa fa-tag"></i> Label {{label.slug}}</h1>
</div>
<div class="list-group">
{{#each label.notes}}
    <a href="/n/{{slug}}" class="list-group-item"><i class="fa fa-file"></i> {{title}}</a>
{{/each}}
</div>
{{>footer.hbs}}